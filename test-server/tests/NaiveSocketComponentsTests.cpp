#include <gmock/gmock.h>
#include <gtest/gtest.h>

#include "BasicTypes.h"
#include "NaiveSocketComponents.h"

using namespace ::testing;

TEST(NaiveSocketComponentsTests, TestConstruct)
{
    // Naive::NaiveSocketComponents
    EXPECT_TRUE(true);
}